// @flow
import EntryContainer from '../entrycontainer';

let component = null;

describe('EntryContainer', () => {
  beforeEach(() => {
    component = <EntryContainer />;
  });

  it('EntryContainer is a div', () => {});
});
