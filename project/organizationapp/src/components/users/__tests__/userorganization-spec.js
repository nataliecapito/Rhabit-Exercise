// @flow
import UserOrganization from '../userorganization';

let component = null;

describe('UserOrganization', () => {
  beforeEach(() => {
    component = <UserOrganization />;
  });

  it('UserOrganization is a div', () => {});
});
